# Guideline-bot
 This application uses a windows library (pywin32) which meant I could not deploy it in streamlit because it's a Linux operating system which does not support window 
 libraries. You can run this code locally in your (Windows) machine!
 Happy coding.
